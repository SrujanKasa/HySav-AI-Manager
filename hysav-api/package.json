{
  "name": "hysav-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HySav | AI Monit backend — workspace, subscription tracking, usage monitoring, waste detection",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "dev": "node --watch src/index.ts",
    "start": "node src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^24.0.0",
    "mongodb-memory-server": "^10.1.3",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
